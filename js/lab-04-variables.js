function lab04() {
    "use strict";

    // YOUR CODE STARTS AFTER THIS LINE:

    // Declare Variables

    // Declare variable of a name
    var myName;

    // Declare variable of a fake address
    var myAddress;

    // Declare variable of an age
    var myAge;

    // Declare variable of a price
    var randomPrice;

      // Assign values into Variables

      // Assign value of 'Tia' to 'myName'
      myName = "Tia"

      // Assign value of '200 Boardwalk Ave, Monopoly'
      // to 'myAddress'
      myAddress = "200 Boardwalk Ave, Monopoly"

      // Assign value of '28' to 'myAge'
      myAge = 28

      // Assign value of '12.99' to 'randomPrice'
      randomPrice = "12.99"

    // Output values to the webpage

    // Output value of 'myName' using my real name
    document.write(myName);

    // Output value on next line
    document.write("<br />");

    // Output value of 'myAddress' using fake/random address
    document.write(myAddress);

    // Output value on next line
    document.write("<br />");

    // Output value of 'myAge 28'
    document.write(myAge);

    // Output value on next line
    document.write("<br />");

    // Output value of 'randomPrice 12.99'
    document.write(randomPrice);



}
